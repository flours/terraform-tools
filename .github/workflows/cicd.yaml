name: Deploy to AWS on PR

on:
  pull_request:
    branches:
      - main  # プルリクエストがマージされる対象ブランチ

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Configure AWS credentials from OIDC
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: arn:aws:iam::211125361567:role/github-actions
          aws-region: ap-northeast-1

      - name: Deploy to AWS
        run: |
          # AWS CLI コマンドまたは Terraform などのツールを使用
          aws s3 ls
